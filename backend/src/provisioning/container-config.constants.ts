/**
 * Container Config Constants
 *
 * BullMQ queue name for agent container configuration sync jobs.
 * Used by ContainerConfigSyncService (producer) and ContainerConfigProcessor (consumer).
 */

export const CONTAINER_CONFIG_QUEUE_NAME = 'container-config';
